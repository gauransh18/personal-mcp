name: Keep Alive

on:
  schedule:
    - cron: '*/4 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping MCP server
        run: curl -s --max-time 10 "${{ secrets.RAILWAY_URL }}/sse" > /dev/null || true
